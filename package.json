{
  "name": "pdf-to-layers",
  "version": "1.0.0",
  "private": true,
  "description": "Photoshop UXP plugin that imports PDF pages as separate layers",
  "scripts": {
    "build": "webpack --mode production",
    "watch": "webpack --mode development --watch",
    "test": "jest",
    "icons": "node scripts/make-icons.js"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "jest": "^30.5.2",
    "webpack": "^5.111.1",
    "webpack-cli": "^7.2.3"
  }
}
